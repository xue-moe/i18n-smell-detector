# VS Code extension template

This template contributes a command that runs:

```bash
npx i18n-smell-detector check --fail-on none
```

It is a starting point for a real extension that could later parse JSON output into diagnostics.
